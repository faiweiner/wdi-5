<h1>New Question</h1>

<iframe src="https://embed.spotify.com/?uri=spotify:track:<%= @selected_track.uri%>" frameborder="0" allowtransparency="true"></iframe>

<% @choices_tracks.each do |track| %>
  <div>
    <%= track.name %>
    <%= track.uri.gsub!('spotify:track:','') if track.name == @selected_track.name %>
  </div>
<% end %>